#
# Copyright (c) 2024 The ZMK Contributors
# SPDX-License-Identifier: MIT
#
# Minimal for vanilla-zmk display isolation test: right has no active
# encoder devicetree node, so EC11 is dropped here (it caused a Kconfig
# warning-as-error on zmk main since DT_HAS_ALPS_EC11_ENABLED=n on this half).

#EXT POWER
CONFIG_ZMK_EXT_POWER=y

# === Basic ===
CONFIG_INPUT=y
